class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        set<int> num;
        for(int i=0;i<nums.size();i++)
        {
            num.insert(nums[i]);
        }
        int i=0;
        for(auto itr : num)
        {
            nums[i]=itr;
            i++;
        }
        return num.size();
    }
};